新增 3D 人物跟随，并支持 DSH 0.1.7-alpha.2，继续兼容此前支持的版本。

- 点击「聚焦」持续跟随人物，按钮显示「聚焦中」；再次点击恢复原来的镜头高度、位置和缩放。
- 跟随时仍可旋转和缩放，侧边栏与悬浮窗切换时保留跟随状态。
- 关闭人物详情或人物离队时自动退出跟随；像素视图保留原有聚焦方式。

在桌面端插件管理中更新 `@zaimokuza/dsh-agent-teams-office@beta`，然后重启桌面端。宿主需启用 Agent Teams。
